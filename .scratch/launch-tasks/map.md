# Map moved to GitHub Issues

The canonical wayfinder map is now **[issue #1](https://github.com/jasonmlarsen/dpcstartuptasks/issues/1)**,
with its tickets as native GitHub sub-issues and blocking expressed as issue dependencies.

Work it with `/wayfinder 1`.

The per-ticket markdown under `issues/` is superseded and kept only as a migration record.
The research findings under `research/` are still live — they are linked from the closed
research issues (#7, #8, #9) but are not duplicated there.
