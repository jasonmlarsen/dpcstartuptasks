# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Office / LibreOffice lock files
~$*
.~lock.*#

# Node
node_modules/
dist/
.env
.env.local
*.log

# Editors
.vscode/
.idea/

# Claude Code local state: machine-local settings and agent worktrees
.claude/

# Wizard run artifacts: raw Kit API responses and captured answers, which
# carry live account data. The findings belong in the ticket, not in git.
.scratch/launch-tasks/kit-provisioning-raw/
.scratch/launch-tasks/kit-provisioning-answers.env
.scratch/launch-tasks/kit-unsubscribe-probe-raw/

.scratch/launch-tasks/kit-resubscribe-form-raw/
.scratch/launch-tasks/kit-resubscribe-form-answers.env

# React Router build output and generated route types
/.react-router/
/build/

# SQLite database files (the persistence layer lives on a volume, never in git)
/data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
